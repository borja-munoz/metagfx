#version 450

// Shadow map fragment shader - depth-only rendering
// No fragment output needed - depth is written automatically by the GPU

void main() {
    // Depth value is written automatically to the depth attachment
    // No color output required for depth-only rendering
}
